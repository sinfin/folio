<% base = Folio::Engine.root.join('app/cells/folio/console') %>
<% Dir.glob("#{base}/**/*.sass").each do |file| %>
<% unless file =~ %r{/_[^/]+\.sass} or file =~ /atoms\/previews\/previews\.sass/ %>
@import 'folio/console/<%= file.gsub("#{base}/", '') %>';
<% end %>
<% end %>
