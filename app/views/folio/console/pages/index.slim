= index_header

= catalogue(@pages)
  ruby:
    edit_link :title
    locale_flag if Rails.application.config.folio_pages_translations
    type
    featured_toggle
    published_toggle
    date(:published_at)

    actions :preview, :edit, :destroy
