= index_header

.f-c-leads-catalogue
  = catalogue(@leads, klass: Folio::Lead)
    ruby:
      show_link(:name, sanitize: true)

      email(sanitize: true)

      date(:created_at)

      state

      actions :show, :edit, :destroy
