.folio-console-index-header
  h1
    - if params[:type] == 'document'
      = t('.documents')
    - else
      = t('.images')

- type = (params[:type] == 'document' ? 'Folio::Document' : 'Folio::Image')
.folio-react-wrap[
  data-file-type=type
  data-mode='index'
]
