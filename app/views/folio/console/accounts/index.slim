= index_header

= catalogue(@accounts)
  ruby:
    edit_link :full_name
    email :email
    attribute(:role) { record.human_role_name }
    attribute :sign_in_count
    date :last_sign_in_at
    actions
