= simple_form_for(@account, as: :account, \
                            url: console_account_path(@account.id), \
                            html: { class: 'form-vertical' }) do |f|
  = render 'form', f: f
