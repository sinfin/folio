ul.nav.nav-tabs
  - [:node, :atoms, :gallery, :documents].each_with_index do |key, i|
    li.nav-item
      a.nav-link[
        data-toggle="tab"
        href="#tab-#{key}"
        class=(i.zero? ? 'active' : nil)
      ]
        = t(".#{key}")
