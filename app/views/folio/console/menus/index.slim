ruby:
  types = @klass.creatable_types if @klass.creatable_types.size > 1

= index_header by_label_query: false,
               types: types,
               new_button: @klass.creatable_types.present?

= catalogue(@menus)
  ruby:
    edit_link :to_label
    type
    locale_flag
    attribute(:menu_items) { record.menu_items.size }
    actions
