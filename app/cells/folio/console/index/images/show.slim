div class=class_name class=id_class_name
  - if options[:cover]
    .f-c-index-images__item(*lightbox(model.cover_placement))
      = admin_image(model.cover_placement, class: 'f-c-index-images__img')

  - elsif options[:custom]
    .f-c-index-images__item(*lightbox_from_image(model))
      = admin_image(model)

  - else
    - model.image_placements.first(3).each do |placement|
      .f-c-index-images__item(*lightbox(placement))
        = admin_image(placement)
