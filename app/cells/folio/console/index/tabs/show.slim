.f-c-index-tabs
  ul.nav.nav-tabs
    - model.each do |tab|
      li.nav-item
        a.nav-link.d-flex.align-items-center[
          href=href(tab)
          class=class_name(tab)
        ]
          = tab[:label]

          - if tab[:count]
            span.f-c-index-tabs__count.badge.badge-pill.ml-2[
              class=count_class_name(tab[:color])
            ]
              = tab[:count]
