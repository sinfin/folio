.form-group.folio-console-nested-model-controls-form-group class=class_name
  - if options[:label]
    label.control-label.d-block
      = options[:label] == true ? '&nbsp;' : options[:label]

  - if handle_position?
    div class=btn_group_class_name
      - %w(up down).each do |direction|
        button.btn.btn-outline-secondary[
          class="folio-console-nested-model-position-button"
          class="fa fa-arrow-#{direction}"
          data-direction=direction
          type="button"
        ]

      = f.hidden_field :position, \
                       class: 'folio-console-nested-model-position-input'

      - if handle_destroy? && options[:vertical]
        = render(:button_destroy)

  - if handle_destroy? && !options[:vertical]
    = render(:button_destroy)
