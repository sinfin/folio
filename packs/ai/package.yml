# Folio AI suggestions pack.
#
# Provides site prompt settings, prompt/user-instruction storage, provider
# adapters, reusable console UI and the centralized HTML-over-wire console API.
# Host applications own field registration and domain-specific model context.

enforce_dependencies: true

dependencies:
  - "../.."
  - "."
