#!/bin/bash

rootDir="$( dirname "$0" )/../.."

(
  cd "$rootDir/tiptap" || exit
  mkdir -p "src/styles/tmp"
  cp "../app/assets/stylesheets/folio/tiptap/_styles.scss" "src/styles/tmp/tiptap-from-app.scss"
)
