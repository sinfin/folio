import styled from 'styled-components'

export default styled.div`
  position: sticky;
  top: 0;
  box-shadow: 0 0 1rem 0.5rem #ffffff;
  background: #ffffff;
  z-index: 2;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 1rem;
  margin-bottom: 1rem;
`
