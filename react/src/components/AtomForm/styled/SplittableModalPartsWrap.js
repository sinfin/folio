import styled from 'styled-components'

export default styled.div`
  border: 1px solid #ced4da;
  padding: 5px 10px;
  background: #ffffff;
  border-radius: .15rem;

  > :first-child {
    margin-top: 0;
  }

  > :last-child {
    margin-bottom: 0;
  }
`
