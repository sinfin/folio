import styled from 'styled-components'

export default styled.div`
  position: relative;

  .folio-loader {
    top: -15px;
    left: -15px;
    width: calc(100% + 30px);
    height: calc(100% + 30px);
    z-index: 2;
  }
`
