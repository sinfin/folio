# Skip preflight check to avoid false positive eslint version conflict warning.
# The project uses local eslint from react-scripts (v6.x) in node_modules,
# but CRA detects a global eslint (v8.57.1) from 'standard' tool in ~/node_modules.
# Node.js module resolution correctly prioritizes local over global, so the build
# uses the correct version. This is safe to keep enabled.
SKIP_PREFLIGHT_CHECK=true

# Port for development server
PORT=3001
