.container-fluid
  h1
    a.<%= classname_prefix %>-unlink href=<%= application_namespace_path %>_ui_path UI kit

  - %w[light dark].each do |theme|
    .bg-body.p-4.mt-4.border.rounded data-bs-theme=theme
      h2.mt-0 Boolean toggles - #{theme}

      h3 No label

      - page = Folio::Page.last

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(record: page,
                                                     url: controller.folio.url_for([:console, page]),
                                                     attribute: :published))

      h3 Label

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(record: page,
                                                     url: controller.folio.url_for([:console, page]),
                                                     attribute: :published,
                                                     label: true))

      h3 Small label

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(record: page,
                                                     url: controller.folio.url_for([:console, page]),
                                                     attribute: :published,
                                                     label: true,
                                                     small_label: true))

      h3 Custom label

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(record: page,
                                                     url: controller.folio.url_for([:console, page]),
                                                     attribute: :published,
                                                     label: "Foo"))

      h3 Verbose labels

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(record: page,
                                                     url: controller.folio.url_for([:console, page]),
                                                     attribute: :published,
                                                     verbose: true))

      h3 Confirmation

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(record: page,
                                                     url: controller.folio.url_for([:console, page]),
                                                     attribute: :published,
                                                     label: "Needs confirmation",
                                                     confirm: true))

      h3 Custom confirmation

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(record: page,
                                                     url: controller.folio.url_for([:console, page]),
                                                     attribute: :published,
                                                     label: "Needs custom confirmation",
                                                     confirm: "Custom confirmation msg?"))

      h3 Disabled

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(record: Folio::Page.last,
                                                     url: controller.folio.url_for([:console, page]),
                                                     attribute: :published,
                                                     disabled: true))

      h3 Without url and record

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(attribute: :published,
                                                     checked: false))

      h3 Without url and record - checked

      = render(<%= application_namespace %>::Ui::BooleanToggleComponent.new(attribute: :published,
                                                     checked: true))
