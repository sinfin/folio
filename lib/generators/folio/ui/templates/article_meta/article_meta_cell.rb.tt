# frozen_string_literal: true

class <%= application_namespace %>::Ui::ArticleMetaCell < ApplicationCell
  def show
    if model[:tag_records].present? || model[:published_at].present?
      render
    end
  end
end
