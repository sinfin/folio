# frozen_string_literal: true

require "test_helper"

class <%= application_namespace %>::Searches::ResultsListCellTest < Cell::TestCase
  test "show" do
    model = create_list(:folio_page, 1)
    html = cell("<%= application_namespace_path %>/searches/results_list", model).(:show)
    assert html.has_css?(".<%= classname_prefix %>-searches-results-list")
  end
end
