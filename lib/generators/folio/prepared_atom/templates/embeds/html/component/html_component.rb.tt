# frozen_string_literal: true

class <%= application_namespace %>::Atom::Embeds::HtmlComponent < ApplicationComponent
  def initialize(atom:, atom_options: {})
    @atom = atom
    @atom_options = atom_options
  end
end
