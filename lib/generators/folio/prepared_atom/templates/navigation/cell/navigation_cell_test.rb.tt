# frozen_string_literal: true

require "test_helper"

class <%= application_namespace %>::Atom::NavigationCellTest < Cell::TestCase
  test "show" do
    atom = create_atom(<%= application_namespace %>::Atom::Navigation, menu: create(:<%= application_namespace_path %>_menu))
    html = cell(atom.class.cell_name, atom).(:show)
    assert html
  end
end
