= render_folio_cache
  = render(Dummy::Ui::HeroComponent.new(title: @topic.to_label,
                                        perex: @topic.perex,
                                        breadcrumbs: @breadcrumbs_on_rails,
                                        show_divider: true,
                                        href: true))
  = render(Dummy::Blog::Articles::IndexComponent.new(topic: @topic,
                                                     articles_scope: @topic.articles))
