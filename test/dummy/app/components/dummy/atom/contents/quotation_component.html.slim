.d-atom.my-atom-lg class=bem_class_name
  .container-fluid
    .d-atom-contents-quotation__inner
      .d-atom-contents-quotation__symbol
        = dummy_ui_icon(:quote,
                        height: 48,
                        class_name: 'd-atom-contents-quotation__symbol-icon')

      .d-atom-contents-quotation__body.fs-text-m
        .d-atom-contents-quotation__text.d-rich-text.d-rich-text--chomp
          == @atom.content

        - if @atom.title.present? || @atom.subtitle.present?
          .d-atom-contents-quotation__meta
            - if @atom.title.present?
              .d-atom-contents-quotation__title
                = @atom.title

            - if @atom.subtitle.present?
              .d-atom-contents-quotation__subtitle
                = @atom.subtitle
