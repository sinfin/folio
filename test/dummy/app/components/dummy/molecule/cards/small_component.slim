.d-molecule.d-molecule-cards-small
  .container-fluid
    .d-molecule-cards-small__flex
      - @atoms.each do |atom|
        .d-molecule-cards-small__card
          = dummy_ui_image molecule_cover_placement(atom),
                           Folio::Console::FileSerializer::ADMIN_THUMBNAIL_SIZE

          = atom.title
