.d-molecule.d-molecule-card-medium
  .container-fluid
    .d-molecule-card-medium__flex
      - @atoms.each do |atom|
        .d-molecule-card-medium__card
          = dummy_ui_image molecule_cover_placement(atom),
                           Folio::Console::FileSerializer::ADMIN_THUMBNAIL_SIZE

          = atom.title
